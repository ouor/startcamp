```bash
git init
git remote add origin https://github.com/ouor/startcamp.git
git remote -v

git add ./README.md
git commit -m "Add README"

git config --global user.email hurwy97@gmail.com
git config --global user.username ouor

git push origin main
```